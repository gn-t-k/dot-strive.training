import { Loading } from "@/app/_components/loading";

import type { FC } from "react";

const PageLoading: FC = () => {
  return <Loading description="初期データを登録しています" />;
};
export default PageLoading;
